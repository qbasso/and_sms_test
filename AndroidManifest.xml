<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.qbasso.smssender"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:restoreAnyVersion="false"
        android:theme="@style/AppTheme" >
        <activity
            android:name="pl.qbasso.activities.ConversationList"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pl.qbasso.activities.SmsConversationActivity"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" >
        </activity>

        <receiver android:name="pl.qbasso.sms.SmsReceiver" >
            <intent-filter android:priority="1000" >
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <activity android:name="pl.qbasso.activities.SendSms" >
        </activity>

        <receiver android:name="pl.qbasso.sms.CustomReceivers$SmsSentReceiver" >
            <intent-filter android:priority="1000" >
                <action android:name="pl.qbasso.SENT" />
                <action android:name="pl.qbasso.RESEND" />
            </intent-filter>
        </receiver>
        <service android:name="pl.qbasso.custom.SendTaskService"></service>
    </application>

</manifest>